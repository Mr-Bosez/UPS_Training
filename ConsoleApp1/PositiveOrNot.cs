using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleApp1
{
    internal class PositiveOrNot
    {

        public void positiveOrNot(int x) {
            Console.WriteLine((x > 0) ? (x + "is a positive Integer") : (x + "is a positive Integer"));
        }
    }
}
